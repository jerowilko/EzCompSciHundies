###################################################
# Targets:
###################################################

#UBUNTU bin path "~/bin"

install:
	UNAME="`uname -a`"
	ifeq [[ "$UNAME" == *"Ubuntu"* ]]; then echo "hi"; fi

###################################################
# Housekeeping:
###################################################

clean:
	test bin/*.class || rm bin/*.class
	test doc/* || rm doc/*
	test test/*.class || rm test/*.class
tar:
	tar cvf Assignment1.tar.gz ./
